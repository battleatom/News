name: News Feed Watchdog (V5 Legacy - Retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The V5 watchdog is retired. V6 production refresh owns live publishing and validation."
