name: Update Market Data (V5 Legacy - Retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy market updates are retired. V6 publishes markets.json as part of the V6 production build."
